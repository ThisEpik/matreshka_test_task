import 'package:flutter/material.dart';

class BattlePassPageManager extends StatelessWidget {
  const BattlePassPageManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
